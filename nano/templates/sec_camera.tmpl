<!--
Title: Security Camera Console (Main content)
Used In File(s): \code\game\machinery\computer\camera.dm
 -->
{{:helper.link('Показать карту', 'thumb-tack', {'showMap' : 1})}}
{{:helper.link('Сбросить', 'refresh', {'reset' : 1})}}
<div class='item'>
    <div class='itemLabel'>Текущая камера: </div>
    {{if data.current}}
        <div class='itemContent'><b>{{:data.current.name}}</b></div>
    {{else}}
        <div class='itemContent'>Нет</div>
    {{/if}}
</div>

{{if data.networks}}
<div class="item">
    <h2>Сети</h2>
    Пожалуйста, выберите сети, которые вы хотели бы мониторить на этой консоли.
    {{if data.emagged}}
        <div class="bad">ВНИМАНИЕ: обнаружен неавторизованный доступ</div>
    {{/if}}
    <div class="item">
        {{for data.networks}}
            {{:helper.link(value.name, value.active ? 'minus' : 'plus', { 'activate' : value.name, 'active' : value.active }, '', value.active ? 'linkOn' : '')}}
        {{/for}}
    </div>
</div>
{{/if}}
<div class="item">
    <h2>Камеры</h2>
    {{for data.cameras}}
        {{if data.current && value.name == data.current.name}}
            {{:helper.link(value.name, '', {'switchTo' : value.camera}, 'selected')}}
        {{else value.deact}}
            {{:helper.link(value.name + " (деактивирована)", '', {}, 'inactive')}}
        {{else}}
            {{:helper.link(value.name, '', {'switchTo' : value.camera})}}
        {{/if}}
    {{/for}}
</div>

