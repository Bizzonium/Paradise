<div class="item">
	<div class="itemLabel">Питание:</div>
	<div class="itemContent">{{:helper.link(data.on ? 'Вкл' : 'Выкл', data.on ? 'power-off' : 'times', {'power' : 1}, null, data.on ? 'selected' : null)}}</div>
</div>
<div class="item">
	<div class="itemLabel">Выходное давление:</div>
	<div class="itemContent">
		{{:helper.link('Указать', 'pencil', {'pressure' : 'input'})}}
		{{:helper.link('Макс', 'plus', {'pressure' : 'max'}, data.pressure == data.max_pressure ? 'disabled' : null)}}
		{{:helper.smoothRound(data.pressure)}} кПа
	</div>
</div>
<div class="item">
	<div class="itemLabel">Фильтр:</div>
	<div class="itemContent">
		{{:helper.link('Ничего', null, {'filter' : -1}, data.filter_type == -1 ? 'selected' : null)}}
		{{:helper.link('Плазма', null, {'filter' : 0}, data.filter_type == 0 ? 'selected' : null)}}
		{{:helper.link('O<sub>2</sub>', null, {'filter' : 1}, data.filter_type == 1 ? 'selected' : null)}}
		{{:helper.link('N<sub>2</sub>', null, {'filter' : 2}, data.filter_type == 2 ? 'selected' : null)}}
		{{:helper.link('CO<sub>2</sub>', null, {'filter' : 3}, data.filter_type == 3 ? 'selected' : null)}}
		{{:helper.link('N<sub>2</sub>O', null, {'filter' : 4}, data.filter_type == 4 ? 'selected' : null)}}
	</div>
</div>

